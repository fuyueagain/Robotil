# “身外化身”动作捕捉与复刻大赛：初赛优化计划

> 文档版本：2026-09-08
> 当前阶段：初赛优化，目标机器人为灵龙 2.0
> 相关实现：WHAM + GMR/Mink IK
> 本计划依据当前代码、84 分官方提交结果及评审意见制定。它是工程执行计划，不是官方评分公式的复刻。

## 0. 先说结论

当前不应把三个优化方案完整地各跑一遍再比较“谁的总分高”。更合理的组织方式是：

| 层级 | 处理方式 | 原因 |
| --- | --- | --- |
| 共同基础 | 先冻结 B84 基线，建立统一评测和固定输入 | 没有共同基线，任何改善或退化都无法归因 |
| 最高优先级主线 | 先诊断并修复后处理器中的脚底接触问题 | 当前代码已明确存在逐帧静态高度调整、无支撑脚 XY 锚定和因果 EMA；它们离滑动/悬空最近 |
| 可并行路线 | 接触感知根部补偿与 GMR/IK 接触约束分别实现、分别评测 | 两条路线修改边界不同，适合做消融和回退 |
| 次要验证 | WHAM 相机/根轨迹、替代模型、复赛实时性并行记录 | 目前没有证据表明它们是本次脚部问题的首要原因；不能抢占初赛主线 |

因此，A 与 B 不是互斥的竞争方案：采用 A 的 benchmark/基线框架，采用 B 的后处理器优先诊断。真正需要比较的是独立的修复路线 R1、R2 及其组合，而不是三个完整 Plan 的“总包”结果。

## 1. 比赛硬约束与已知证据

### 1.1 官方规则

| 项目 | 约束 | 对计划的影响 |
| --- | --- | --- |
| 初赛评分 | 动作相似度（关节角度 RMSE、DTW）与物理可行性 | 初赛先解决脚掌滑动/悬空，同时设置动作保持门槛 |
| 复赛评分 | 实时帧率、跟踪延迟、稳定性、流畅性、跨演示者泛化 | 复赛能力只做接口和耗时记录，不提前牺牲初赛效果 |
| 输入 | 仅允许单目 RGB；禁止深度相机 | 可使用单目深度估计，但不得依赖深度传感器 |
| 实时要求 | 处理 30 FPS，端到端延迟不超过 200 ms | 所有离线优化都必须标注能否因果化 |
| 机器人 | 仅灵龙 2.0，使用组委会提供的 URDF/MJCF | 几何、关节限位、碰撞和接触阈值以该模型为准 |
| 开源算法 | 允许 | 记录代码版本、权重来源、许可证及 SHA256 |

### 1.2 执行环境门槛

本项目的 WHAM、GMR、benchmark、测试和性能测量统一运行在 Conda 环境 `wham_gmr` 中。环境不满足时，不得把运行结果、失败信息或性能数据纳入实验结论。

每次运行项目、测试或 benchmark 前，先确认解释器来自目标环境：

```powershell
conda activate wham_gmr
python -c "import os, sys; assert os.environ.get('CONDA_DEFAULT_ENV') == 'wham_gmr'; print(sys.executable)"
```

在未激活环境或脚本由外部进程启动时，使用显式环境调用：

```powershell
conda run --no-capture-output -n wham_gmr python -c "import os, sys; assert os.environ.get('CONDA_DEFAULT_ENV') == 'wham_gmr'; print(sys.executable)"
conda run --no-capture-output -n wham_gmr python -m pytest -q tests/test_ground_clearance.py
```

执行规范如下：

| 规则 | 要求 |
| --- | --- |
| Python/pytest | 使用 `wham_gmr` 环境中的 `python` 和 `python -m pytest`；禁止直接调用系统 `python` 或 `pytest` |
| 项目入口 | `demo.py`、`handle_wham_gmr.py`、`scripts/` 和 benchmark 均通过已确认的 `wham_gmr` 解释器启动 |
| 结果记录 | 报告记录 `CONDA_DEFAULT_ENV`、`sys.executable`、Python 版本、关键依赖版本和 Git commit |
| 环境失败 | 环境检查失败时立即停止，不将该次输出与 B84 或候选版本比较 |

### 1.3 当前可确认的结果

| 证据 | 当前事实 | 解释边界 |
| --- | --- | --- |
| 官方结果 | 初赛总分 84 | 未提供 RMSE、DTW、物理可行性分项，不能反推官方权重 |
| 官方意见 | 脚掌滑动、脚部悬空 | 是当前最高优先级缺陷，但没有给出可直接计算的扣分量 |
| 当前入口 | `run.ps1` 默认 `SMOOTH_ALPHA=0.35`、`HEIGHT_ADJUST=1`、`ROOT_ORIGIN_OFFSET=0`、`CAMERA_FOLLOW=1` | `OnlineQposPostprocessor` 中的 `smooth_alpha=0.25` 只是类默认值，不等于 B84 历史配置 |
| 当前后处理 | 每帧按全身 FK 最低 body 高度做静态 `height_adjust`；没有支撑脚状态，也没有脚底 XY 锚定；根部和关节使用因果 EMA | 这些是滑动/悬空的高优先级根因假设，仍需 D0-D3 消融验证 |
| 脚部模型 | 灵龙脚由踝 mesh 和细圆柱组成，不是平板；最低几何点约在踝部 body 下方 0.076 m | `ground_clearance=0.075` 只能作为几何初值，不能宣称保证动态接触 |
| WHAM 集成 | 流式链路中 `cam_angvel=0`，并使用 `refine_traj=False` | 当前链路没有启用 DPVO 动态相机旋转补偿或 WHAM 轨迹细化 |
| GMR | 运行时是带权 Mink IK，不是训练神经网络；人体 pelvis 映射到机器人 `base_link`，根部位置权重为 100；模型有 freejoint | 根部没有被 GMR 硬锁死；`qpos[:3]` 的变化必须单独检查 |

## 2. B84 基线冻结

官方提交目录作为只读参考：

`D:\Robot-imitation-learning\大师赛第一期赛题1—跑得快-王莹莹—18540293911`

### 2.1 必须冻结的内容

| 类别 | 必须记录 |
| --- | --- |
| 输入 | 视频文件、分辨率、帧率、总帧数（已知为 661 帧/约 22 s）、裁剪和关键点预处理 |
| 模型 | WHAM/GMR/SMPL-X 版本、模型文件、权重来源、许可证、SHA256 |
| 映射 | 灵龙 2.0 MJCF/URDF、关节顺序、坐标变换、人体到机器人映射配置 |
| 后处理 | `smooth_alpha`、`height_adjust`、`ground_clearance`、`root_origin_offset`、平滑顺序、异常值处理 |
| 输出 | `my_motion.pkl`、`live_motion.csv`、元数据、预览视频及生成命令 |
| 环境 | Python/依赖版本、GPU/CPU、提交代码 Git commit |

当前入口默认值只能作为“可复现当前代码”的记录；在确认 84 分提交时的历史命令前，不得把它们标为 B84 的确切参数。若历史参数无法恢复，建立两个标签：`B84-reported`（官方产物）和 `B84-reproduced`（当前代码重跑产物）。

### 2.2 B84 的基线报告

输出一份机器可读报告（JSON/CSV 均可）和一份人工可读摘要，至少包含：脚底最低点曲线、脚底接触候选曲线、左右脚 XY 轨迹、根部 XYZ 轨迹、关节限位/自碰撞统计、每阶段耗时。报告中的所有阈值、坐标系和关节顺序必须显式记录。

## 3. 内部 benchmark：代理评测，不冒充官方分数

官方没有公开三项子分数和公式，因此本 benchmark 只回答“候选版本是否比 B84 在已知缺陷上更好、是否破坏了动作”，不能预测下一次官方分数，也不能把内部指标线性换算成 0–100 官方分。

### 3.1 五组指标

| 维度 | 指标 | 用途 |
| --- | --- | --- |
| 足部接触 | 脚底穿地率、支撑期悬空率、支撑脚速度、支撑漂移、接触切换抖动、双脚异常离地率 | 直接响应官方反馈 |
| 动作保持 | 候选相对 B84 的关节角 RMSE、分部位 DTW、关键动作幅度保持率 | 防止修脚破坏原有动作；不是官方真值 |
| 目标匹配 | 机器人 FK 与 SMPL-X/GMR 目标的位置/方向残差、末端轨迹 DTW | 检查机器人是否仍跟随人物 |
| 物理与时序 | 关节越限、自碰撞、速度/加速度/jerk、根部抖动 | 评估物理可行性与流畅性 |
| 性能记录 | 吞吐 FPS、端到端延迟 p50/p95/p99、丢帧率、跟踪重启次数 | 为复赛保留工程证据，不作为当前主线门槛 |

### 3.2 接触参考与脚底几何

1. 从灵龙 2.0 MJCF/URDF 的实际几何计算左右脚底候选点/最低点，并记录 body frame 到几何点的偏移。不能直接把 `0.015 m` 或 `-0.005 m` 当作最终阈值。
2. 在固定 661 帧视频上人工标注少量但覆盖完整周期的状态：左支撑、右支撑、双支撑、腾空/换步、不确定。标注结果是接触检测器的参考，不是三维滑动真值。
3. 接触检测使用滞回和最短持续帧数：低脚底高度、较小垂直速度、较小水平速度同时满足才进入支撑；退出使用更宽松条件，避免逐帧抖动。
4. 脚底最低点用于穿地/离地；连续滑动应跟踪定义明确的脚底接触点或脚底几何采样点，不能把踝部 body 原点直接当作脚掌。
5. 用人工标注计算接触状态 precision/recall；在检测器未通过基本验证前，不使用其结果宣称“滑动已修复”。

### 3.3 动作相似度的保护规则

动作保持指标按部位报告：根部/躯干、髋、膝、踝/脚、肩、肘、腕。上身和躯干使用紧容许度；腿脚允许因接触修复产生必要变化，但必须同时满足“接触指标改善 + 步态节奏不被破坏 + 目标匹配残差不过度增加”。不能简单把腿脚全部锁定在有缺陷的 B84 上，也不能以修脚为由忽略动作退化。

DTW 前统一帧率、帧区间、关节顺序、角度单位、四元数顺序、坐标系和尺度；角度差使用周期环绕。至少计算：候选 vs B84、机器人 FK vs SMPL-X/GMR 目标、接触优化前 vs 优化后。

### 3.4 验收门槛

在官方细分权重未知时，使用门槛而非综合分：

| 门槛 | 要求 |
| --- | --- |
| 物理安全 | 穿地率不增加；自碰撞和关节越限不增加；无新增异常帧 |
| 脚部效果 | 标注支撑期的悬空率、支撑脚速度和支撑漂移均较 B84 有明确改善；改善幅度和置信区间写入报告 |
| 动作保持 | 上身/躯干 RMSE 与 DTW 不超过预设退化容许度；腿脚变化必须能由接触改善解释 |
| 目标跟随 | FK-目标残差不出现明显恶化，关键跑步周期不丢失 |
| 可复现性 | 相同输入、模型和参数重复运行结果一致，输出带版本与配置清单 |

只有在积累多个具有官方分数的独立提交后，才考虑用回归分析探索内部指标与官方总分的关系；一次 84 分样本不具备拟合意义。

## 4. 主线阶段与依赖关系

### P0：冻结基线与数据契约

**目标**：让所有后续实验可复现、可比较。

**任务**：补全 B84 配置；确认原始 GMR qpos 是否可导出；统一时间轴、关节顺序、坐标系和 MJCF；生成 B84-reported/B84-reproduced 报告；记录离线处理各阶段耗时。

**出口条件**：同一输入重复运行输出一致；所有超参和版本可追溯；明确哪些数据只有官方产物、哪些是当前代码重跑。

### P1：脚部几何标定与后处理器根因诊断（最高优先级）

**目标**：先回答滑动/悬空究竟由哪一处理环节造成。

**任务**：完成脚底几何测量和人工接触标注；绘制 root Z、root XY、左右脚底 z/XY、支撑状态；对同一原始序列执行四组消融：

| 实验 | EMA | 静态 `height_adjust` | 目的 |
| --- | --- | --- | --- |
| D0 | 关 | 关 | 原始 IK 的问题 |
| D1 | 开 | 关 | EMA 对接触和滞后的影响 |
| D2 | 关 | 开 | 每帧高度校正对支撑/腾空的影响 |
| D3 | 开 | 开 | 当前组合行为 |

**出口条件**：确认 D0–D3 的贡献；完成接触检测 precision/recall；阈值来自 MJCF 几何与误差敏感性实验，而非照抄占位数值。

### P2：R1 接触感知根部位置补偿

**假设**：支撑脚期间，脚底世界 XY 应近似固定，根部应补偿人体目标与机器人当前脚底之间的误差；腾空期解除锁定，不能把摆动脚钉在地面。

**实现顺序**：接触状态机 → 支撑脚锁定点 → 根部 XY 补偿 → 根部 Z 仅按支撑脚几何与地面约束调整 → 换脚时软切换/滞回 → 因果低延迟平滑。

**约束**：不再使用“所有 body 最低点每帧静态钉到固定高度”作为唯一高度策略；对双支撑定义加权或优先脚规则；对腾空动作保留必要根部高度。

**验收**：优先看支撑脚速度、支撑漂移、悬空率和穿地率；同时检查躯干/上身动作保持与目标残差。

### P3：R2 将接触约束加入 GMR/Mink IK

**假设**：如果后处理根补偿仍无法同时满足关节目标和脚底接触，可在 IK 目标层加入接触任务。

**实现顺序**：增加左右脚位置任务及接触权重 → 根部与脚部任务协调 → 关节限位/自碰撞约束 → 接触切换的权重渐变 → 与 R1 做正交消融。

**风险**：脚部权重过高会牺牲动作角度 RMSE/DTW；支撑状态错误会把错误锁定放大。必须在 P1 的接触参考通过后实施，不能直接调高脚部权重碰运气。

### P4：候选组合与动作保持回归

比较 `B84`、`R1`、`R2`、`R1+R2` 及必要的轻量参数变体。每个候选使用独立输出目录和配置快照，禁止覆盖 B84。使用统一 benchmark 生成对照表，按“物理改善优先、动作退化受控”的门槛选出提交候选。

### P5：WHAM 根轨迹/相机与替代模型验证

**WHAM/相机**：标准离线 WHAM 可经 DPVO 估计相机角速度，但当前流式链路 `cam_angvel=0`。先用固定序列对比启用/不启用相机旋转补偿对 root XY/Z 和脚部指标的影响；不要在没有实验数据时把相机问题定性为脚部主因。相机平移仍不是由现有 DPVO 旋转接口直接解决。

**替代模型**：仅在 P1–P4 已有可复现报告后，对替代姿态/跟踪模型做小规模 A/B；记录许可证、权重和耗时。替代模型不是当前脚部修复的前置条件。

### P6：复赛迁移与实时性保留

把离线模块标记为：

| 模块 | 离线能力 | 因果化要求 |
| --- | --- | --- |
| 接触状态 | 可用未来帧平滑 | 使用滞回、最短持续帧和有限窗口；记录判定延迟 |
| 支撑脚锁定 | 可做全序列优化 | 在线只使用当前/历史观测，换脚允许有限延迟 |
| DTW/零相位滤波 | 仅评测/离线 | 不放入实时推理路径 |
| EMA/因果滤波 | 在线可用 | 测量其 p50/p95 延迟和动作滞后 |

最终粗测必须达到 30 FPS、端到端延迟不超过 200 ms，并记录 p50/p95/p99、丢帧率和跟踪重启次数。初赛提交不应为了未验证的复赛假设而放弃脚部物理改善。

## 5. 并行执行方式

P0 和 P1 是共同前置，不能并行跳过。P1 完成接触参考后，可以并行开发：

| 路线 | 修改边界 | 交付物 | 合并条件 |
| --- | --- | --- | --- |
| R1 | `scripts/smplx_to_robot_stream.py` 后处理/根估计 | 接触状态机、脚底锚定、根部补偿、因果版本 | 接触指标改善且上身动作不退化 |
| R2 | GMR/Mink IK 任务与权重 | 脚部任务、权重渐变、限位/碰撞约束 | 比 R1 在统一 benchmark 上有增益，或解决 R1 无法解决的残差 |
| R3 | WHAM/DPVO 相机接口 | 相机补偿 A/B 和根轨迹报告 | 只有在数据证明相机因素显著时才进入主线 |
| R4 | 实时性能 | 各阶段 profiling、因果替代方案 | 不改变初赛候选的动作和接触结果 |

每条路线必须从同一 B84 输入开始，输出到独立目录，并提交配置、日志、benchmark 报告和失败案例。禁止仅凭预览视频判断根部是否锁定；需要同时检查 `qpos[:3]`、脚底几何和接触指标。

## 6. 风险、决策与停止规则

| 风险 | 识别信号 | 处理 |
| --- | --- | --- |
| 接触误判 | 标注集 precision/recall 低，换脚频繁抖动 | 先修检测器和滞回，不进入 IK 锁定 |
| 静态补偿造成悬空 | 腾空期 root Z 被拉低，脚底离地率上升 | 降级或移除 `height_adjust`，改用接触条件约束 |
| EMA 造成滞后 | 支撑切换时脚底速度峰值和姿态滞后增加 | 调参或只对非根部自由度平滑；保留原始序列对照 |
| 脚部修复破坏动作 | 上身 RMSE/DTW 或目标残差超门槛 | 回退脚部权重/约束，分离上身和腿脚容许度 |
| 相机假设误导 | 启用 DPVO 后脚部指标无改善或恶化 | 将相机补偿保留为验证结果，不阻塞 R1/R2 |
| 离线方案无法实时化 | 依赖未来帧、延迟超过 200 ms | 保留因果简化版；离线版本不得直接宣称可用于复赛 |
| B84 参数不可恢复 | 官方产物与当前重跑不一致 | 双标签记录，禁止伪造历史参数；以可复现重跑作为实验基线 |

停止/回退条件：任何候选若穿地、自碰撞或上身动作保持明显恶化，即使脚部某一指标变好也不合并；任何“改善”若无法在相同输入和重复运行中复现，不作为结论。

## 7. 当前阶段的最终交付清单

1. B84 配置快照与 `B84-reported`/`B84-reproduced` 区分说明。
2. 灵龙 2.0 脚底几何标定文件和人工接触参考标注。
3. D0–D3 后处理消融报告，包含 root/脚底曲线和耗时。
4. R1、R2 独立实现及统一 benchmark 对照表；如无法实现，记录阻塞原因而非跳过评测。
5. 初赛候选的验收记录：脚部物理改善、上身动作保持、目标匹配和可复现性。
6. 复赛迁移备注：哪些步骤是离线的、哪些步骤已因果化、30 FPS/200 ms 的实测结果或当前缺口。

## 8. 对外表述规范

不要写：

> “0.075 m 离地补偿保证脚底不与地面穿插。”

应写：

> “依据灵龙 2.0 脚部几何设置初始地面补偿；动态接触质量通过脚底几何、支撑期滑动、悬空和穿地指标验证。当前版本仍需接触感知根部/IK 约束来改善官方指出的问题。”

不要写“GMR 学习到了目标动作”或“GMR 把根部固定”。准确表述是：WHAM 估计人体姿态和根轨迹，GMR 通过带权 IK 将人体目标重定向到灵龙 2.0；根部是否移动以输出 `qpos[:3]` 和脚底世界轨迹为准。
