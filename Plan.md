# “身外化身”动作捕捉与复刻大赛：初赛冲刺计划

  > 更新时间：2026-09-03
  > 当前目标：先稳定通过初赛并进入复赛。
  > 当前输入：examples/dataset_video.mp4
  > 当前 baseline：WHAM 人体运动恢复 + GMR/SMPL-X 到灵龙 2.0 的运动重映射。

  ## 1. 阶段目标与总体策略

  比赛三个阶段是递进关系，共享“视频 → 人体姿态 → 机器人关节”的核心底座：

   阶段      主要评分                           当前策略
  ━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   初赛      关节角度 RMSE、DTW、物理可行性     最高优先级：优化固定视频的离线
                                                结果
  ────────  ─────────────────────────────────  ─────────────────────────────────
   复赛      FPS、延迟、连续性、跨演示者泛化    保留逐帧接口，后续替换为因果低
                                                延迟版本
  ────────  ─────────────────────────────────  ─────────────────────────────────
   总决赛    真机任务完成、自然度、遮挡鲁棒     初赛和复赛通过后再投入
             性、创新性

  当前初赛阶段不把文本指令生成、真机接口、多摄像头和复杂模仿学习作为主线。这些能
  力属于后续阶段，不会直接提升固定测试视频上的初赛分数。

  ## 2. 当前 baseline 推理链

  examples/dataset_video.mp4
          ↓
  WHAM：单目视频人体运动恢复
          ↓
  SMPL/SMPL-X 人体姿态与时序运动
          ↓
  GMR：人体骨架到灵龙 2.0 的运动学重映射
          ↓
  IK / qpos 后处理
          ↓
  关节限位、平滑、物理修正
          ↓
  机器人关节角度序列 + MuJoCo 演示视频

  ### 2.1 P0 阶段必须核实的模型信息

  当前可以确认项目使用了 WHAM 和 GMR 相关链路，但具体模型和版本必须从代码、下载
  脚本与运行日志中核实：

  | 核验项 | 需要记录 |
  | --- | --- |
  | 人体检测器 | 名称、版本、输入分辨率、检测频率 |
  | 2D 关键点模型 | 名称、关键点格式、置信度处理方式 |
  | WHAM checkpoint | 文件名、来源、SHA256、许可证 |
  | SMPL/SMPL-X 模型 | 模型版本、性别模型、关节定义 |
  | GMR 配置 | 人体到灵龙的关节映射、零位偏置、轴方向 |
  | 推理环境 | GPU、CPU、内存、显存峰值、PyTorch/CUDA 版本 |
  | 阶段耗时 | 检测、WHAM、GMR、后处理、写盘分别耗时 |

  在这些信息核实前，不要直接把其他数据集上的公开 benchmark 当作本项目分数依据。
  比赛最终评价的是灵龙机器人关节序列的 RMSE、DTW 和物理可行性。

  ## 3. WHAM 的优势、弱点与替换原则

  ### 3.1 当前保留 WHAM 的原因

  | 优势 | 对比赛的意义 |
  | --- | --- |
  | 使用视频时间上下文 | 减少逐帧姿态抖动，改善动作连续性 |
  | 能恢复人体整体运动 | 对转身、走动和躯干运动更有帮助 |
  | 面向视频人体运动恢复 | 与固定 RGB 视频输入形式匹配 |
  | 已经接入当前 GMR | 可先做增量优化，工程风险较低 |

  ### 3.2 可能的弱点

  | 弱点 | 对比赛的风险 |
  | --- | --- |
  | 模型推理较重 | 本机运行速度可能不足以直接用于复赛 |
  | 单目深度存在不确定性 | 前后方向、尺度和根部高度可能漂移 |
  | 训练数据和比赛视频存在域差异 | 不同衣着、视角和遮挡可能造成姿态偏差 |
  | SMPL/SMPL-X 与灵龙骨架不同 | 人体动作正确不代表机器人关节正确 |
  | 预训练模型不负责机器人约束 | 仍需要 GMR、IK、限位、接触和自碰撞后处理 |

  当前原则：初赛先不直接替换 WHAM。只有误差分析证明 WHAM 是主要瓶颈时，才进入替
  代模型对照实验。

  ## 4. 初赛验收指标

  | 指标 | 验收要求 |
  | --- | --- |
  | 输出完整性 | 每帧都有合法关节序列，无缺帧、NaN，时间戳单调 |
  | 动作相似度 | RMSE 和 DTW 持续改善 |
  | 关节角度 | 不超过灵龙 2.0 关节限位 |
  | 角速度 | 无明显单帧跳变 |
  | 角加速度 | 高频抖动受控，快速动作仍然保留 |
  | 物理可行性 | 无明显自碰撞、严重穿地、膝关节反折或躯干失稳 |
  | 可复现性 | 相同输入、配置和随机种子得到一致结果 |

  每次实验都应记录：

  ```text
  实验编号、代码 commit、模型 checkpoint、配置文件
  输入视频、输出文件、总耗时、峰值显存
  关节角 RMSE、DTW、最大速度、最大加速度
  越限帧数、自碰撞帧数、穿地帧数、失败帧数
  最差动作片段、异常原因和最终结论

  ## 5. 执行路线

  ### P0：建立可重复的 baseline 报告

  1. 固定 examples/dataset_video.mp4、输出目录和随机种子。
  2. 从代码和日志提取检测器、2D 姿态模型、WHAM checkpoint、SMPL-X 模型和 GMR 配
     置。

  3. 分别计时检测、WHAM、GMR、后处理和视频写盘。
  4. 记录本机 GPU/CPU 使用率、显存峰值和总运行时间。
  5. 统计输出帧数、FPS、NaN、丢帧、检测失败和追踪重启。
  6. 统计每个机器人关节的角度范围、速度和加速度。
  7. 生成 baseline CSV/JSON 报告和 MuJoCo 演示视频。

  验收标准：同一环境连续运行两次，输出帧数、时间戳和关节序列一致；所有模型来源、
  版本和权重都可追溯。

  ### P1：固定视频专项优化

  每次只改一个主要因素，并进行单变量消融实验。

  1. 坐标系和初始姿态校准

     确认相机坐标、人体前方向、灵龙前方向、左右肢体符号、关节轴方向和初始零位。

  2. 人体比例归一化

     使用肩宽、髋宽、躯干长度和腿长做比例归一化，避免不同体型直接造成机器人动作
     幅度失真。

  3. 躯干和根部校准

     单独优化根部旋转、根部高度、水平漂移和躯干朝向，避免根部误差污染全身动作。

  4. 关节映射偏置

     针对肩、肘、髋、膝、腕等高误差关节，建立可解释的方向修正和零位偏置。

  5. 离线零相位滤波

     初赛允许使用完整视频，可以使用前向-反向零相位滤波，降低噪声而不引入相位延
     迟。

  6. 异常帧修复

     检测单帧大跳变，使用邻近帧插值或短窗口优化，避免异常姿态直接传给机器人。

  7. 角度、速度和加速度约束

     对最终 qpos 增加角度限位、最大速度和最大加速度约束，并记录每一步对 RMSE、
     DTW 和物理指标的影响。

  误差分析不能只看整段视频平均值，应按以下部位拆分：

  躯干、肩部、肘部、腕部、髋部、膝部、脚部

  同时保存误差最大的帧和最差动作片段。

  ### P2：遮挡、快速动作和深度稳定性

   问题              处理方案
  ━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   短时遮挡          使用历史状态、关键点置信度和插值，不立即重置整段轨迹
  ────────────────  ──────────────────────────────────────────────────────
   快速动作          对高置信度且连续的快速动作降低滤波强度，避免过度平滑
  ────────────────  ──────────────────────────────────────────────────────
   单目深度抖动      分别滤波根部深度、躯干朝向和肢体长度
  ────────────────  ──────────────────────────────────────────────────────
   检测丢失          设置最大容忍帧数，超过阈值后再重初始化，并统计次数
  ────────────────  ──────────────────────────────────────────────────────
   视角变化          将相机方向估计与人体朝向估计分开，避免混淆
  ────────────────  ──────────────────────────────────────────────────────
   关键点置信度低    降低该帧更新权重，更多使用历史状态和运动模型

  ### P3：GMR/IK 与物理可行性

  1. 明确灵龙 2.0 每个关节的角度限位、速度上限和加速度上限。
  2. 针对肩、肘、髋、膝处理人体和机器人构型差异。
  3. 对末端位置、关节姿态和躯干姿态使用分层权重。
  4. 加入脚底接触、支撑脚锁定和躯干姿态约束。
  5. 输出前做关节越限、自碰撞和明显穿地检查。
  6. 保存以下中间结果，便于定位误差来源：

  原始 qpos
  → 限位 qpos
  → 速度/加速度约束 qpos
  → 平滑 qpos
  → 最终 qpos

  #### 脚底陷入地面问题

  脚底陷入地面属于局部物理可行性问题，优先级低于主映射精度，但提交前必须修复并回
  归验证。

  处理顺序：

  1. 先完成坐标、比例、映射和时序优化。
  2. 根据灵龙 2.0 MJCF 中脚部几何体的最低点校准 ground_clearance。
  3. 验证穿地帧数、支撑稳定性、躯干高度和 RMSE。
  4. 补偿值只对确认存在同类偏差的机器人生效，不能直接泛化到所有机器人。

  ### P4：替代预训练模型对照

  候选模型必须使用同一输入视频、同一 GMR、同一后处理和同一评测脚本进行公平比较。

   候选方向  WHAM（当前）
   可能优势  视频时序、整体运动、已有工程链路
   主要风险  较重、单目深度漂移、域差异
   使用建议  先优化，作为基准
  ──────────────────────────────────────────────────────────────────────────────
   候选方向  GVHMR 等全局运动模型
   可能优势  根部速度、行走和转身可能更稳定
   主要风险  接口、依赖和权重成本较大
   使用建议  P1 后对照
  ──────────────────────────────────────────────────────────────────────────────
   候选方向  4D-Humans/HMR2
   可能优势  单帧姿态和遮挡对照价值
   主要风险  时序抖动、根部运动不一定稳定
   使用建议  对照或补帧器
  ──────────────────────────────────────────────────────────────────────────────
   候选方向  ViTPose
   可能优势  2D 关键点精度可能更高
   主要风险  推理速度和显存开销较大
   使用建议  初赛离线精度实验
  ──────────────────────────────────────────────────────────────────────────────
   候选方向  RTMPose
   可能优势  延迟低、适合实时
   主要风险  极限姿态精度可能低于大模型
   使用建议  复赛候选
  ──────────────────────────────────────────────────────────────────────────────
   候选方向  MediaPipe Pose
   可能优势  速度快、部署简单
   主要风险  3D 和遮挡鲁棒性有限
   使用建议  备用低算力链路

  替代模型只有同时满足以下条件才值得替换 WHAM：

  - RMSE 更低；
  - DTW 更好；
  - 物理修正更少；
  - 本机运行时间可接受；
  - 权重和依赖可以离线封装；
  - 许可证允许比赛使用和提交。

  单独 FPS 更高但动作相似度下降，不算有效改进。

  ### P5：为复赛预留实时接口

  初赛阶段就保持以下模块边界：

  视频读取
  → 人体估计
  → 时序状态
  → 重映射
  → 约束
  → 机器人关节输出

  将“逐帧推理”和“离线零相位后处理”分离。进入复赛后：

  - 将零相位滤波替换为因果滤波；
  - 限制时序窗口长度；
  - 使用检测降频和跟踪补帧；
  - 测量真实端到端延迟；
  - 记录 p50、p95、p99 延迟，而不是只记录平均 FPS。

  ## 6. 本机资源分配建议

   方向                       建议投入比例    原因
  ━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   baseline 评测和数据记录             15%    没有基线就无法判断改动是否有效
  ─────────────────────────  ──────────────  ────────────────────────────────
   坐标、比例和关节映射                30%    最可能直接改善 RMSE 和 DTW
  ─────────────────────────  ──────────────  ────────────────────────────────
   时序、异常帧和遮挡处理              25%    同时改善连续性和鲁棒性
  ─────────────────────────  ──────────────  ────────────────────────────────
   GMR/IK/接触约束                     20%    降低越限、自碰撞和穿地
  ─────────────────────────  ──────────────  ────────────────────────────────
   替代模型探索                        10%    先验证瓶颈，再决定是否扩大投入

  ## 7. 实验规则与记录模板

  实验规则：

  1. 一次只改一个主要因素。
  2. 固定输入视频、输出格式、随机种子和后处理顺序。
  3. 同时记录动作分数、物理指标和耗时。
  4. 保存配置文件和 checkpoint 哈希。
  5. 对最差片段生成可视化。
  6. “看起来更自然”必须经过数值指标确认。
  7. 如果 RMSE 变好但物理可行性变差，保留两套结果并分析权衡，不直接覆盖
     baseline。

   实验编号  B0
   改动      原始 baseline
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  E1
   改动      坐标系和零位校准
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  E2
   改动      人体比例归一化
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  E3
   改动      离线零相位滤波
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  E4
   改动      异常帧修复
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  E5
   改动      关节限位/速度约束
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  E6
   改动      脚底接触补偿
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论
  ──────────────────────────────────────────────────────────────────────────────
   实验编号  M1
   改动      替代模型对照
   RMSE
   DTW
   越限帧
   自碰撞帧
   穿地帧
   总耗时
   结论

  ## 8. 风险、停止条件与提交物

   风险              预警信号              处理方式
  ━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   盲目更换大模型    公开指标很好，但机    使用同一 GMR/后处理公平消融
                     器人 RMSE 不变或变
                     差
  ────────────────  ────────────────────  ──────────────────────────────────────
   过度平滑          DTW 变好，但快速动    按关节和动作速度自适应滤波
                     作幅度明显变小
  ────────────────  ────────────────────  ──────────────────────────────────────
   后处理掩盖根因    限位或修复帧比例持    回查坐标、比例和关节映射
                     续升高
  ────────────────  ────────────────────  ──────────────────────────────────────
   固定视频过拟合    轻微裁剪或速度变化    提交前做简单扰动回归测试
                     后就失败
  ────────────────  ────────────────────  ──────────────────────────────────────
   依赖无法封装      checkpoint、许可证    提前固定权重、版本和离线启动脚本
                     或网络下载不明确
  ────────────────  ────────────────────  ──────────────────────────────────────
   实时性不足        离线运行时间远超视    P0 记录各阶段耗时，P1 后进行实时粗测
                     频时长

  出现以下情况时，停止继续堆模型，先修基础链路：

  - 输出存在 NaN、缺帧或时间戳错误；
  - 左右肢体方向或机器人坐标系尚未确认；
  - 大量帧依赖硬裁剪才能合法；
  - 物理不可行性来自脚底、根部或关节零位的确定性错误；
  - 没有可重复的数值评测；
  - 模型权重或依赖无法离线封装。

  初赛提交物：

  1. 动作序列文件；
  2. 使用说明；
  3. MuJoCo 演示视频；
  4. 配置、模型权重和许可证说明；
  5. 技术报告；
  6. baseline 与最终版本的指标对比；
  7. 明确帧率、关节顺序、角度单位和坐标约定。

  ## 9. 最终决策原则

  先核实 WHAM/GMR 的真实配置
  → 建立固定视频 baseline 数值报告
  → 优先修坐标、比例、关节映射和时序问题
  → 加入限位、接触和自碰撞约束
  → 使用统一评测比较 GVHMR、4D-Humans、ViTPose、RTMPose 等候选
  → 选择综合分数最高且可封装的方案

  对当前目标“稳进复赛”而言，这条路线比直接重写整套人体估计系统更可控。

  脚底陷入地面是一个小而明确的物理约束问题，按 P3 处理即可。它不应阻塞 P0/P1 的
  主线评测和映射精度优化，但在提交前必须纳入物理可行性回归测试。